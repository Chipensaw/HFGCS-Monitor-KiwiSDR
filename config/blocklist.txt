# HFGCS recorder -- receiver opt-out list.
#
# One entry per line. Matched against the receiver's host or host:port as it
# appears in the survey cohort. Blank lines and lines beginning with # are
# ignored.
#
# Any sysop who asks to be excluded goes here, no questions and no delay.
# A wildcard host with no port excludes every port on that host.
#
# Format:
#   example.ddns.net:8073      exclude one receiver
#   example.ddns.net           exclude all receivers on that host
#
# Entries below this line are live.

# Jumonville PA. Excluded 2026-08-02: heaviest powerline noise in the
# 200-receiver survey cohort (37.5 dB, verified twice at two
# frequencies). Produced 11 false captures in one night before the
# detector learned to reject 120 Hz mains harmonics. The detector now
# rejects the hum, but the receiver still cannot usefully hear HFGCS,
# so holding one of its 6 slots serves nobody.
w3piesdr.ddns.net
